// pending
#include <bits/stdc++.h>
using namespace std;
int main()
{

    int t;
    cin >> t;
    while (t--)
    {
        bool flag = true;
        int n;
        cin >> n;
        vector<int> v;
        for (int i = 0; i < n; i++)
        {
            int temp;
            cin >> temp;
            v.push_back(temp);
        }
        if(v[0]==1)cout<<"YES"<<endl;
        else cout<<"NO"<<endl;
        
    }
}